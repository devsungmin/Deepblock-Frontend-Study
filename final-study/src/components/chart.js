import bb from "billboard.js";

var data = [
    {
        // input json data type
    }
]
var chart = new bb.generate{
    bindto: '#JSONData',
    data: {
        // json: data,
        // keys: { value: [ ] }
    }
}